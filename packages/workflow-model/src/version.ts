/**
 * Version of the workflow document: the definition the editor saves and the `.runflux.json` envelope
 * carries as `schemaVersion`. Changing its shape incompatibly means a new version and a migration.
 */
export const WORKFLOW_SCHEMA_VERSION = 1;
